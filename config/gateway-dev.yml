spring:
  cloud:
    gateway:
      routes:
      - id: neo_route
        uri: http://www.baidu.com
        filters:
        - AddRequestHeader=X-Request-Foo, Bar
        predicates:
        - Path=/test
